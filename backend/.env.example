# Copy to backend/.env and edit as needed.

# "sqlite" (default, no container runtime required) or "postgres"
DB_BACKEND=sqlite

# Used only when DB_BACKEND=postgres; matches docker-compose.yml
POSTGRES_URL=postgresql+psycopg2://prahari:prahari@localhost:5432/prahari

ENGINE_VERSION=0.1.0
